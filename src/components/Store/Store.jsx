import React from 'react';
import PropTypes from 'prop-types';

const Store = (props) => {
    return (
        <div>
            search
            filter
            list items with link to detail view
        </div>
    );
}

Store.propTypes = {
};

export default Store;